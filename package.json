{
  "name": "llm",
  "version": "0.0.1",
  "description": "A nextjs template to build dashboard",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "lint": "turbo lint",
    "build": "turbo build",
    "start": "turbo start",
    "clean": "turbo clean && rm -rf node_modules .turbo"
  },
  "dependencies": {
    "@isomorphic/core": "workspace:*"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "@repo/typescript-config": "workspace:*",
    "@repo/tailwind-config": "workspace:*",
    "rimraf": "^5.0.5",
    "turbo": "2.0.1"
  },
  "packageManager": "pnpm",
  "engines": {
    "node": ">=18"
  }
}